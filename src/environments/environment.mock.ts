//Mock aonde colocamos a url real mas ela sera interceptada pelos interceptores
export const environment = {
  production: false,
  mockEnabled: true,
  apiBaseUrl: 'https://jsonplaceholder.typicode.com'  
};
